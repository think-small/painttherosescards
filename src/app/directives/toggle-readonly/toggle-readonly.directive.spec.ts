import { ToggleReadonlyDirective } from './toggle-readonly.directive';

describe('ToggleReadonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleReadonlyDirective();
    expect(directive).toBeTruthy();
  });
});
