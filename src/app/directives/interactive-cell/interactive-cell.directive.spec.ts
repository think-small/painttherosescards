import { InteractiveCellDirective } from './interactive-cell.directive';

describe('CellClickDirective', () => {
  it('should create an instance', () => {
    const directive = new InteractiveCellDirective();
    expect(directive).toBeTruthy();
  });
});
